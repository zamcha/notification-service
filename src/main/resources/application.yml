spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: "/"
  mail:
    host: smtp.gmail.com
    port: 587
    username: caotran293@gmail.com
    password: rygclrhxlmtpuoar   # nên dùng App Password của Gmail thay vì mật khẩu thật
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
  application:
    name: notification-service
  config:
    #    Imports configuration data from a config server when available
    import: "configserver:http://localhost:6969"
    cloud:
      config:
        uri: ${CONFIG_SERVER_URL:http://localhost:6969}
        #      1
        request-connect-timeout: 5000
        #      2
        request-read-timeout: 5000
        #      3
        fail-fast: true
        retry:
          #        4
          max-attempts: 10
          #        5
          initial-interval: 1000
          #        6
          max-interval: 2000
          #        7
          multiplier: 1.1
          #1 Timeout on waiting to connect to the config server (ms)
          #2 Timeout on waiting to read configuration data from the config server (ms)
          #3 Makes a failure to connect to the config server fatal
          #4 Maximum number of attempts
          #5 Initial retry interval for backoff (ms)
          #6 Maximum retry interval for backoff (ms)
          #7 Multiplier to compute the next interval
server:
  port: 8003
queue:
#  notification:
#    queue: notification-queue
#    exchange: notification-exchange
#    routing-key: notification-routing-key
  notification-mall:
    queue: notificationmall-queue
    exchange: notificationmall-exchange
    routing-key: notificationmall-routing-key
  notification:
    queue: notification-email1-queue       # Tên hàng đợi
    exchange: notification-email1-exchange # Tên bưu điện
    routing-key: notification-email1-key   # Mã định tuyến